#include "holberton.h"

/**
 * _strlen - string lenght
 *
 * Description: returns the length of a string
 * Return: Alawy (0)
 */

int _strlen(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; ++i)
	{
		continue;
	}

	return (i);
}
