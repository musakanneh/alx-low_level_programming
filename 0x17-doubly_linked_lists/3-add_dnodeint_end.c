#include "lists.h"

/**
 * add_dnodeint_end - a function that adds a new node at
 * the end of a dlistint_t list
 *
 * Return: the address of the new element, or NULL if it failed
 */

stint_t *add_dnodeint_end(dlistint_t **head, const int n)
{
	return ();
}
